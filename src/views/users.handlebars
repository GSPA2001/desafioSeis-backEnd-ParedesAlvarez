<ul class="list-group">
    {{#each data.docs}}
        <li class="list-group-item">
            {{this.first_name}} {{this.last_name}} - {{this.email}} - {{this.age}}
        </li>
    {{/each}}
</ul>